#!/bin/sh
# Check if Node.js version is compatible
node_version=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
if [ "$node_version" -lt 18 ]; then
  echo "Skipping pre-commit hooks: Node.js $node_version < 18"
  exit 0
fi

npx lint-staged